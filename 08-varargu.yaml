- name: variables from arugments
  hosts: local
  connection: local
  tasks:
  - name: print the course info
    ansible.builtin.debug:
     msg: "course name is : {{course}}, dur :{{duration}}, studentname: {{student}}"